public class Main {
    public static void main(String[] args) {
        FizzBuzz.main(args);
//        FizzBuzz.main(); fixes proposed are bad because
        // it either removes the arguments of FizzBuzz.main
        //making it a normal method or it removes the body of
        //main.

        // For git pull example
    }
}
